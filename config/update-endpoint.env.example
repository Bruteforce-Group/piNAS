# Copy to /etc/pinas/update-endpoint.env on each piNAS client.
# scripts/manage-clients.sh setup-key writes this file automatically.
# Leave the values blank if you are preparing an image manually; the helper will
# replace them with real credentials during provisioning.
WORKER_URL=""
CLIENT_ID=""
CLIENT_TOKEN=""
